<ul class="nav__list">
  <!-- prettier-ignore -->
  <% [ {name: "das Café", target: "cafe"}, 
       {name: "Herkunft", target: "origins"},
       {name: "Shop", target: "shop"}].forEach(function(item){ %>
  <li
    class="menu-item <%if (locals.activePage == item.target ) { %>active<% } %>"
  >
    <a href="<%=`${item.target}.ejs`%>" class="menu-link"><%= item.name %></a>
  </li>
  <% }); %>
</ul>
